# Env Vault 待办

## 全局搜索（标签功能完成后实施）

- [ ] 前置依赖：完成标签定义、Secret group/item 关联、权限控制及维护接口。
- [ ] 第一阶段使用 PostgreSQL 实现 key、备注、标签的授权范围内全局检索。
- [ ] 支持项目、文件夹、环境筛选，明确 group 去重与 item 统计口径。
- [ ] 明确 group/item 标签的继承和匹配语义后，再实现标签筛选及分类统计。
- [ ] value 当前加密存储；实际值检索另行设计，不因标签分类自动开放或建立明文索引。
- [ ] 使用代表性百万级数据验证首屏延迟、分页、并发和权限隔离，再按瓶颈决定是否引入独立搜索引擎。

搜索涉及 Vue 前端和 Go 后端；若调整 SDK 使用的响应契约，同步检查 Java SDK 兼容性。

# Env Vault 待办

## 全局搜索

- [x] 新增秘钥检索页面交互预览：多范围级联复选、同一项目内的环境复选和搜索结果空态，设计见 design/secret-search.md
- [x] 检索结果合并单元格与历史弹框 Mock 预览：按范围和密钥合并，一个环境一行，版本详情及批次详情在同一弹框切换
- [x] 接入独立 secret/search 接口，实现 Key、备注包含匹配、多范围和环境筛选、组级分页、取消旧请求与错误重试
- [x] 检索页历史接入真实分页追加和按需批次详情，独立预览入口保留 Mock 数据
- [ ] 后续提取管理页与检索页共用的历史展示组件
- [x] 租户标签定义、Secret groupId 标签绑定和解绑、密钥列表展示与目录内标签筛选
- [ ] 前置依赖：标签与密钥接口接入统一权限控制
- [x] 第一阶段使用 PostgreSQL pg_trgm 实现 Key、备注搜索，仅解密当前页，total 按 groupId 去重
- [ ] 增加全局标签候选与标签筛选，并接入统一资源和环境权限；当前共享密钥查询仅要求认证
- [x] 标签当前仅绑定 groupId，跨环境共享；目录内多标签筛选按任意一个匹配
- [ ] 全局搜索的标签分类统计；单环境标签仅在业务明确需要时再设计
- [ ] value 当前加密存储；实际值检索另行设计，不因标签分类自动开放或建立明文索引。
- [x] 独立 PostgreSQL 测试验证 10 万和 100 万条虚构环境实例的索引计划及查询耗时，结论见后端设计文档
- [ ] 在目标 PostgreSQL 版本及代表性生产数据验证端到端首屏延迟、深分页、并发和权限隔离，再按瓶颈决定是否引入独立搜索引擎

搜索涉及 Vue 前端和 Go 后端；若调整 SDK 使用的响应契约，同步检查 Java SDK 兼容性。
